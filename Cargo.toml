[workspace]
members = [
    "backlight",
    "luksctl",
    "notifi",
    "search",
    "wallpaper",
    "writeback",
]
resolver = "3"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = true
opt-level = 3
