[package]
name = "notifi"
version = "0.2.1"
edition = "2024"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
serde_json = "1.0"
shared = {path = "../shared"}
zbus = { version = "4.0", default-features = false, features = ["blocking", "tokio"] }
