[package]
name = "backlight"
version = "0.2.1"
edition = "2024"
description = "A utility to control external monitor brightness through `ddcutil`."

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true }
